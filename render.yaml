services:
  - type: web
    name: rdb
    env: docker
    plan: free
    disk:
      name: data-disk
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: DATA_DIR
        value: /data
    buildCommand: docker build -t rdb .
    startCommand: docker run -p 8080:8080 rdb
